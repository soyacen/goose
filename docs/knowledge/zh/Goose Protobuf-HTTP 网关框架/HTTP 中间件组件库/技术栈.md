- **JWT 认证**：使用 `github.com/golang-jwt/jwt/v5` 进行令牌解析与签发。
- **可观测性**：集成 `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp` 实现自动化的 HTTP 追踪与指标采集。
- **自适应限流**：`limiter` 子包实现了基于 BBR 算法的自适应限流，不依赖外部 Redis 等存储，纯内存滑动窗口统计。