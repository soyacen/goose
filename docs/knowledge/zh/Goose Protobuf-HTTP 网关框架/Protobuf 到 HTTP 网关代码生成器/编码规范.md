- 所有生成的服务端和客户端组件均支持通过 Options 模式注入中间件（Middleware）链，以实现横切关注点的解耦。
- 请求参数解析遵循严格的类型映射规则，路径参数仅支持标量类型及 Wrapper 类型，不支持复杂的嵌套 Message 或 Map。
- 采用统一的错误处理机制，服务端通过 `ErrorEncoder` 统一输出错误响应，客户端通过 `ErrorDecoder` 还原业务错误。