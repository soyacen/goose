- 所有配置方法（如 SetQuery, SetHeader）在执行前均检查 `s.err` 状态，若存在前置错误则直接返回，确保错误在链式调用中不被覆盖且延迟到 Send 时统一抛出。
- 请求体（Body）设置方法（如 JSONBody, XMLBody）在序列化成功后会自动设置对应的 `Content-Type` 头部，并在可能时计算并设置 `Content-Length`。
- 响应解析采用统一的 `ObjectBody` 模板方法，接收自定义 unmarshal 函数，在此基础上封装了 JSONBody, XMLBody, ProtobufBody 等具体格式解析器。